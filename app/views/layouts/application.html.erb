<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%= csrf_meta_tag %>
    <%= render 'layouts/stylesheets' %>
  </head>
  <body>
    <div class="container">
      <%= render 'layouts/header' %>
      <section class="round">
        <%= yield %>
      </section>
	  <%= render 'layouts/footer' %>
	  <%# this is to have a debug panel at the bottom of the pages %>
	  <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>